# Leviathan Core — sample inventory (public products, for demo/repro)
# ONLY register assets you own or are contracted to assess.
attestation: authorized
owner_contact: sec@acme.test

assets:
  # CPE-precision example (confidence: cpe)
  - identifier: mail.acme.test
    cpe: cpe:2.3:a:microsoft:exchange_server:2019:*:*:*:*:*:*:*
    keywords: ["exchange server"]

  # keyword-level example (confidence: keyword — vendor agreement enforced)
  - identifier: vpn.acme.test
    vendor: ivanti
    product: connect secure
    version: "9.1R12"
    keywords: ["connect secure", "ics"]

  - identifier: files.acme.test
    vendor: progress
    product: moveit transfer
    keywords: ["moveit transfer", "moveit"]
